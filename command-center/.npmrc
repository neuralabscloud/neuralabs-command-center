# Some transitive deps (composio-core / langchain / @langchain/*) declare
# conflicting peer ranges that npm 7+ refuses to resolve strictly. Accept the
# known-good resolution so `npm install` works without --legacy-peer-deps flags.
legacy-peer-deps=true
